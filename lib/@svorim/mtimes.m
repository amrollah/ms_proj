function c = mtimes(a,b)
error('multiplication not implemented');
% if isnumeric(b) && numel(b)==1, c=a; c.lc=a.lc*b;
% elseif isnumeric(a) && numel(a)==1, c=b; c.lc=b.lc*a;
% else error('models can be multiplied only by scalars'); 
% end
